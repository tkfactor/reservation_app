<div class="container">
<h3 class="my-3 ml-3">登録済みルーム一覧</h3>

<table class="table table-hover">
  <thead>
    <tr class="py-2">
      <th>ルーム画像</th>
      <th>ルーム名</th>
      <th>ルーム紹介</th>
      <th>料金</th>
      <th>作成日</th>
    </tr>
  </thead>
  <tbody>
   <% @rooms.each do |room| %>
    <tr>
      <td><%= image_tag room.room_image, size: "240x140" if room.room_image.present? %></td>
      <td><%= link_to room.room_name, room %></td>
      <td><%= room.room_introduction %></td>
      <td><%= room.room_price %></td>
      <td><%= room.created_at.to_s(:datetime_jp)%></td>
    </tr>
   <% end %>
  </tbody>
</table>
</div>
